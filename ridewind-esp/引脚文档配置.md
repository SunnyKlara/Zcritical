2.4寸的圆形屏幕240x240，spi驱动芯片是gc9a01，scl接的7引脚，sda接的6引脚，dc接的5引脚，cs接的4引脚。
音频数据输入DIN(IO13)，音频位时钟BCLK(IO12)，音频左 / 右时钟LRC(IO11)接的max98357。
LED1 (IO41)LED控制接了10个ws2812b的灯珠，LED2 (IO16)LED 控制 接了4个ws2812b的灯珠。
S1(IO17)开关 / 编码器引脚，S2(IO18)开关 / 编码器引脚，接的是ec11 360度20位编码器，KEY(IO8)按键引脚拉低触发。
mos1管控制超声波加湿器ch1(IO10)只需要控制开关，mos2管控制风扇需要调速ch2(IO40)。